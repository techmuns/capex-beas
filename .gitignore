# Dependencies (installed with --no-save in CI; never committed)
node_modules/
package-lock.json

# Local env / secrets — NEVER commit
.env
.env.*
.dev.vars

# Scratch / temp
tmp/
*.tmp
.DS_Store

# Local, throwaway demo fixture for eyeballing the POPULATED dashboard (?demo=1).
# Never committed — the shipped public/data/*.json stay empty until Actions fill them.
public/demo/

# Wrangler / Cloudflare local state
.wrangler/

# Logs
*.log
npm-debug.log*
